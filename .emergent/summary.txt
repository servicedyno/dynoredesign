<analysis>**original_problem_statement:**
The initial request was to set up and install dependencies for the project. Following that, the user made a series of feature requests and bug reports:
1.  **Auto-generate Names:** API name and wallet name should have a short, random, friendly name auto-generated if not provided by the user, instead of being set to .
2.  **Shorter Names:** The auto-generated names should be made shorter.
3.  **Currency Consistency:** The user initiated a deep dive into how  works, leading to a major effort to make the entire application (dashboard, transactions, wallets, emails, PDFs, API responses) consistent with the company's selected .
4.  **API Key Rules:**
    *   A company should only have one API key per environment (one for production, one for development).
    *   The  for both keys must be the same.
    *   The **production key** acts as the master source of truth for the currency. If a production key is created or updated with a new currency, the development key should be automatically synced to match it.
5.  **FIAT Currency Restriction:**  for API keys must be a FIAT currency, restricted to a specific list of 14 supported currencies.
6.  **Cache Consolidation:** The dual in-memory and Redis cache system should be simplified to a single Redis-based cache.
7.  **API Response Formatting:** All endpoints returning monetary values should include a consistent currency info object ().
8.  **Token Expiry:** API responses for authenticated routes should include a header indicating when the token will expire.
9.  **Payment Link Creation:** The user asked several clarifying questions about payment link creation, Admin Tokens, and what should happen if a user has no active API key. The final pending request is to ensure users without an active API key cannot create payment links.

**User's preferred language**: English

**what currently exists?**
The application is a full-stack monolith with a React frontend and a FastAPI (TypeScript) backend using a PostgreSQL database and Redis for caching. All dependencies are installed and services are running.

The application now enforces a sophisticated set of rules around API keys and currency management. A company's base currency, determined by its production API key, dictates the currency for all financial data displayed across the system. This consistency has been implemented end-to-end, from backend controllers to email templates and PDF invoices. The API has been enhanced to provide rich, consistently formatted currency information in its responses. The caching layer has been simplified to use only Redis.

**Last working item**:
*   **Last item agent was working:** The agent was addressing the user's final request: to confirm that a user cannot create a payment link if they have deleted their API key and to implement this restriction if it doesn't already exist.
*   **Status:** IN PROGRESS
*   **Agent Testing Done:** N
*   **Which testing method agent to use?** backend testing agent
*   **User Testing Done:** N

**All Pending/In progress Issue list**:
*   **Issue 1:** The  endpoint might allow link creation even if a company has no active API key. (Priority: P0)

**Issues Detail:**
*   **Issue 1:**
    *   **Attempted fixes:** The agent has confirmed that the route  is protected by , meaning a valid user token is required. However, it has not yet verified if the controller logic itself checks for the existence of an active API key for the user's company before proceeding.
    *   **Next debug checklist:**
        1.  Analyze the  function in .
        2.  At the beginning of the function, add logic to retrieve the company's active API key (similar to the logic in ).
        3.  If no active API key is found, immediately return a 400 error with a user-friendly message, such as An active API key is required to create a payment link. Please create one in your developer settings.
    *   **Why fix this issue and what will be achieved with the fix?** This will ensure that all payment-related actions are tied to a properly configured company profile with an active API key, preventing orphaned or misconfigured payment links.
    *   **Status:** IN PROGRESS
    *   **Is recurring issue?** N
    *   **Should Test frontend/backend/both after fix?** backend
    *   **Blocked on other issue:** None

**In progress Task List**:
*   **Task 1:** Complete the implementation of the check in the  endpoint. (Priority: P0)
    *   **Where to resume:** Open  and modify the  function.
    *   **What will be achieved with this?** The application will correctly prevent users without an active API key from creating payment links.
    *   **Status:** IN PROGRESS
    *   **Should Test frontend/backend/both after fix?** backend
    *   **Blocked on something:** None

**Upcoming and Future Tasks**
*   **Upcoming Tasks:**
    *   None. All high-priority user requests have been addressed or are in progress.
*   **Future Tasks:**
    *   **Task 1 (P1):** Implement a public, unauthenticated endpoint for creating payment links, potentially using the legacy  header for authentication. This was discussed as a possible enhancement.
    *   **Task 2 (P1):** Implement a workflow to automatically create a default company and a USD API key when a new user registers.
    *   **Task 3 (P2):** Update the frontend components to consume the new, consistent  objects returned by the backend for displaying all monetary values.

**Completed work in this session**
*   **Dependency Management:** Installed all required npm <command>

Usage:

npm install        install all the dependencies in your project
npm install <foo>  add the <foo> dependency to your project
npm test           run this project's tests
npm run <foo>      run the script named <foo>
npm <command> -h   quick help on <command>
npm -l             display usage info for all commands
npm help <term>    search for help on <term>
npm help npm       more involved overview

All commands:

    access, adduser, audit, bugs, cache, ci, completion,
    config, dedupe, deprecate, diff, dist-tag, docs, doctor,
    edit, exec, explain, explore, find-dupes, fund, get, help,
    help-search, hook, init, install, install-ci-test,
    install-test, link, ll, login, logout, ls, org, outdated,
    owner, pack, ping, pkg, prefix, profile, prune, publish,
    query, rebuild, repo, restart, root, run-script, sbom,
    search, set, shrinkwrap, star, stars, start, stop, team,
    test, token, uninstall, unpublish, unstar, update, version,
    view, whoami

Specify configs in the ini-formatted file:
    /root/.npmrc
or on the command line via: npm <command> --key=value

More configuration info: npm help config
Configuration fields: npm help 7 config

npm@10.8.2 /usr/lib/node_modules/npm and 
Usage:   
  pip <command> [options]

Commands:
  install                     Install packages.
  lock                        Generate a lock file.
  download                    Download packages.
  uninstall                   Uninstall packages.
  freeze                      Output installed packages in requirements format.
  inspect                     Inspect the python environment.
  list                        List installed packages.
  show                        Show information about installed packages.
  check                       Verify installed packages have compatible 
dependencies.
  config                      Manage local and global configuration.
  search                      Search PyPI for packages.
  cache                       Inspect and manage pip's wheel cache.
  index                       Inspect information available from package 
indexes.
  wheel                       Build wheels from your requirements.
  hash                        Compute hashes of package archives.
  completion                  A helper command used for command completion.
  debug                       Show information useful for debugging.
  help                        Show help for commands.

General Options:
  -h, --help                  Show help.
  --debug                     Let unhandled exceptions propagate outside the
                              main subroutine, instead of logging them to
                              stderr.
  --isolated                  Run pip in an isolated mode, ignoring
                              environment variables and user configuration.
  --require-virtualenv        Allow pip to only run in a virtual environment;
                              exit with an error otherwise.
  --python <python>           Run pip with the specified Python interpreter.
  -v, --verbose               Give more output. Option is additive, and can be
                              used up to 3 times.
  -V, --version               Show version and exit.
  -q, --quiet                 Give less output. Option is additive, and can be
                              used up to 3 times (corresponding to WARNING,
                              ERROR, and CRITICAL logging levels).
  --log <path>                Path to a verbose appending log.
  --no-input                  Disable prompting for input.
  --keyring-provider <keyring_provider>
                              Enable the credential lookup via the keyring
                              library if user input is allowed. Specify which
                              mechanism to use [auto, disabled, import,
                              subprocess]. (default: auto)
  --proxy <proxy>             Specify a proxy in the form
                              scheme://[user:passwd@]proxy.server:port.
  --retries <retries>         Maximum attempts to establish a new HTTP
                              connection. (default: 5)
  --timeout <sec>             Set the socket timeout (default 15 seconds).
  --exists-action <action>    Default action when a path already exists:
                              (s)witch, (i)gnore, (w)ipe, (b)ackup, (a)bort.
  --trusted-host <hostname>   Mark this host or host:port pair as trusted,
                              even though it does not have valid or any HTTPS.
  --cert <path>               Path to PEM-encoded CA certificate bundle. If
                              provided, overrides the default. See 'SSL
                              Certificate Verification' in pip documentation
                              for more information.
  --client-cert <path>        Path to SSL client certificate, a single file
                              containing the private key and the certificate
                              in PEM format.
  --cache-dir <dir>           Store the cache data in <dir>.
  --no-cache-dir              Disable the cache.
  --disable-pip-version-check
                              Don't periodically check PyPI to determine
                              whether a new version of pip is available for
                              download. Implied with --no-
                              index.
  --no-color                  Suppress colored output.
  --use-feature <feature>     Enable new functionality, that may be backward
                              incompatible.
  --use-deprecated <feature>  Enable deprecated functionality, that will be
                              removed in the future.
  --resume-retries <resume_retries>
                              Maximum attempts to resume or restart an
                              incomplete download. (default: 5) packages.
*   **Auto-Generated Names:** Implemented a friendly name generator () for API keys and wallets.
*   **End-to-End Currency Consistency:**
    *   Refactored , , , , , and  to display all monetary values in the company's .
    *   Created a centralized currency utility  to handle formatting and provide consistent currency info objects in API responses.
*   **API Key Management Overhaul:**
    *   Enforced a limit of one production and one development key per company in .
    *   Implemented logic for the production key's currency to be the master, automatically syncing the development key.
    *   Restricted  to a list of 14 supported FIAT currencies.
    *   Implemented a fallback mechanism to use the last known currency if no active key exists, ensuring UI stability.
*   **Cache Simplification:** Consolidated the dual in-memory/Redis cache system to a Redis-only implementation in .
*   **API Enhancements:**
    *   Added a  header to authenticated responses ().
    *   Created a new endpoint  to assist the frontend with currency selection UX.
*   **Database Cleanup:** Wrote and executed scripts to revoke duplicate API keys and update keys with non-FIAT base currencies.
*   **Clarifications Provided:** Answered user questions regarding JWT vs. Admin tokens, token usage for payment link creation, and the user onboarding flow.

**Earlier issues found/mentioned but not fixed**
*   None. All identified issues were addressed during the session.

**Known issue recurrence from previous fork**
*   N/A

**Code Architecture**


**Key Technical Concepts**
*   **Backend:** FastAPI with TypeScript ()
*   **Frontend:** React
*   **Database:** PostgreSQL with Sequelize
*   **Caching:** Redis
*   **Authentication:** JWT (user session and long-lived Admin Token)

**key DB schema**
*   **tbl_api:** 
*   **tbl_user_transactions:** 
*   **tbl_companies:** 

**changes in tech stack**
*   The caching mechanism was simplified from a dual-layer (in-memory + Redis) system to a Redis-only architecture.

**All files of reference**
*   **Created:**
    *   : Generates random names for API keys/wallets.
    *   : Central utility for currency formatting, symbol lookup, and resolving a company's effective base currency.
*   **Significantly Updated:**
    *   : Core logic for API key creation, currency rules, and the new  endpoint.
    *   , , , : Updated to fetch and display data according to the company's .
    *   : Refactored to remove the in-memory cache.
    *   : Modified to add the token expiry header.

**Areas that need refactoring**:
*   The logic for querying a company's preferred currency (the complex  clause) has been duplicated across several controllers. This should be consolidated into a single exported function in  and called from each controller to reduce code duplication and improve maintainability.

**key api endpoints**
*   : Creates API keys, enforcing all new currency and uniqueness rules.
*   : Provides currency options to the frontend for a given company.
*   : Returns currency-aware dashboard statistics.
*   : Returns a list of currency-aware transactions.
*   : Returns currency-aware wallet information.
*   : Creates a new payment link. **(Currently being modified)**.
*   : Backend health check.

**Critical Info for New Agent**
*   **Production Key is Master:** The  of a company is strictly determined by its **production** API key. The development key is always automatically synced to match it. This logic is in .
*   **Currency Fallback is Crucial:** When no *active* API key exists, the system gracefully falls back to the currency of the *most recently created* key (even if revoked/deleted). This ensures the UI remains consistent. The specific SQL query for this is the source of truth and is used in multiple controllers.
*   **Use Currency Utils:** For any new feature involving money, you **must** use the helpers in  to get the company's currency and to format display values. This ensures consistency with the rest of the application.
*   **Single Redis Cache:** All cache interactions are now  and go directly to Redis. The old synchronous in-memory cache is gone.

**documents and test reports created in this job**
*    (updated multiple times)

**Last 10 User Messages and any pending HUMAN messages**
1.  **User:** Asks how to enforce that production and development keys share the same currency, suggesting a UX flow. - **Status:** In Progress
2.  **User:** Refines the rule: the production key's currency should be the master and automatically update the development key. - **Status:** In Progress
3.  **User:** Yes (approving the refined plan). - **Status:** Implemented
4.  **Agent:** Provides a summary of the implemented currency sync logic. - **Status:** Completed
5.  **User:** Asks to confirm that a user with a deleted API key cannot create payment links and should be prompted to create a new key. - **Status: PENDING (This is the active task)**
6.  **Agent:** Begins investigation into the payment link creation flow. - **Status:** In Progress
7.  ... (Agent continues investigation steps)
8.  **Agent:** Confirms that payment link creation requires a token but hasn't yet checked for an active API key. - **Status:** In Progress

**Project Health Check:**
*   **Broken:** None
*   **Mocked:** None

**3rd Party Integrations**
*   None added in this session.

**Testing status**
*   **Testing agent used after significant changes:** YES
*   **Troubleshoot agent used after agent stuck in loop:** NO
*   **Test files created:** None
*   **Known regressions:** None

**Credentials to test flow:**
*   Use the  user, as this account's companies (e.g., company_id 38) and API keys have been central to testing all new features.

**What agent forgot to execute**
*   The agent has not yet implemented the final check in the  controller to block requests from users who do not have an active API key for their company. This is the main pending task.</analysis>
