# ============================================
# DynoPay Backend - Python Dependencies
# ============================================
# This file lists all Python packages required for the backend
# Install with: pip install -r requirements.txt

# ============================================
# Core Framework (ASGI Server)
# ============================================
uvicorn[standard]==0.25.0          # ASGI server for running server.py
starlette==0.37.2                  # ASGI framework (compatible with fastapi)

# ============================================
# HTTP Client Libraries
# ============================================
httpx==0.28.1                      # Async HTTP client for proxy requests
requests==2.32.3                   # Sync HTTP client for API calls

# ============================================
# Environment & Configuration
# ============================================
python-dotenv==1.0.0               # Load environment variables from .env

# ============================================
# Data & Caching
# ============================================
redis==5.0.1                       # Redis client for caching and sessions

# ============================================
# Optional Testing Dependencies
# ============================================
# Uncomment if running test scripts
# pytest==7.4.3                    # Testing framework
# pytest-asyncio==0.21.1           # Async testing support

# ============================================
# Notes
# ============================================
# - uvicorn[standard] includes WebSockets, watchfiles, and other extras
# - httpx is used for the lightweight proxy in server.py
# - redis is used in test scripts for Redis integration testing
# - python-dotenv loads .env file variables
# - All versions are pinned for reproducibility

# ============================================
# Installation Commands
# ============================================
# Development:
#   pip install -r requirements.txt
#
# Production:
#   pip install -r requirements.txt --no-cache-dir
#
# Upgrade all:
#   pip install -r requirements.txt --upgrade
