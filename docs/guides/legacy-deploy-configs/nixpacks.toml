[phases.setup]
nixPkgs = ["nodejs_20", "yarn"]

[phases.install]
cmds = ["yarn install --ignore-engines"]

[phases.build]
cmds = ["yarn build", "mkdir -p logs"]

[start]
cmd = "node dist/server.js"

[variables]
NODE_ENV = "production"
