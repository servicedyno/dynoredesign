[phases.setup]
nixPkgs = ["nodejs_20", "yarn"]

[phases.install]
cmds = ["cd backend && yarn install --ignore-engines"]

[phases.build]
cmds = ["cd backend && yarn build && mkdir -p logs"]

[start]
cmd = "cd backend && node dist/server.js"

[variables]
NODE_ENV = "production"
