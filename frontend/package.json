{
  "name": "dynopay-frontend-bridge",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "cd /app && node_modules/.bin/next start -p 3000 -H 0.0.0.0",
    "build": "cd /app && node_modules/.bin/next build",
    "dev": "cd /app && node_modules/.bin/next dev --turbo -p 3000 -H 0.0.0.0"
  }
}
